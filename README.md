# Short-Range-Wireless-Communication
A Python-based client-server application that enables reliable file transfer over a TCP network. The client sends the selected file’s name, size, and data, while the server receives and stores it securely. This project highlights socket programming, data transmission, and efficient file handling in Python.
